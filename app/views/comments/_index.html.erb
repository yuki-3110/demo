<!-- <ul> -->
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <!-- <li> -->
      <!-- 追記 -->
        <div class="comment-content" id ="this_comment_<%= comment.id %>"></div>
      <!-- ここまで -->
            <%= comment.content %>
            <!-- 追記 -->
            <td><%#= link_to "編集", edit_blog_comment_path(blog, comment), remote: true, class: 'btn btn-primary btn-sm' %></td>
            <!-- ここまで -->
            <%= link_to blog_comment_path(comment.blog_id, comment.id), class: 'btn btn-primary btn-sm', method: :delete, remote: true, data: { confirm: "本当に削除しますか?"} do %>
              <i class="fa-solid fa-trash-can"></i>
            <% end %>
        

      <!-- </li> -->
    <% end %>
  <% end %>
<!-- </ul> -->
